---
name: douni-customer-review-backfill
description: 刘朵或遥师手动选择每日提报日期范围，读取四路客户终审结果，回填机构及原日表，并只把客户通过结果按机构发送推进执行卡；不同于半小时机构镜像。
---

# 客户审核结果回填

入口为“回填客户结果并通知机构”或“客户审核结果回填”。仅刘朵或遥师可在内部会话发起，且须由原发起人确认。

## 范围卡

1. 先发送一张 Card 2.0 范围卡，选择每日提报日期的开始、结束日期，可选填唯一 ID。
2. 只处理所选日期对应的新增合作、历史台账回执和同范围失败通知，不自动扩大到全部历史。
3. 范围卡本身不读表、不回填、不通知。提交范围后生成唯一一张预览卡及一份完整明细表，禁止逐条刷屏。

## 读取与关联

1. 读取四个客户提报页的终态结果，再结合长期提交台账定位原合作。
2. 正常路径使用台账、客户提报日期、选题、档期、平台和端口重定位；客户行号只作缓存。结果回填阶段允许对缺失本地日期的历史合作恢复关联，这是兜底，不混进普通新增提报。
3. 机构表按机构＋唯一 ID 定位，达人名称允许标点、空格、emoji 的轻微差异；主页链接不作为机构记录判定条件。客户页多期仍须用日期、选题、档期等区分，无法唯一对应则单条待核。

## 回填规则

- 客户审核“是” → 机构及原每日提报记录“提报审核＝客户通过”。
- 客户审核“否” → “提报审核＝客户不通过”。
- 空白、待定等非终态不覆盖已有结果。
- 客户反馈写入“提报客户反馈”。
- 小红书若客户通过但要求换选题，提报审核仍写“客户通过”，把换选题、方向和客户备注合并进“提报客户反馈”，不新增审核状态，也不自动改选题。

顺序固定为：客户页读取 → 机构表回填并回读 → 原每日提报表回填并回读 → 在原内部会话生成机构通知预览 → 原发起人二次确认 → 对客户通过项发送机构推进卡。第一次确认只执行回填，不得直接触发机构群消息。全量表继续由原机构同步更新，不直接反向写全量表。

每个写入阶段都必须在原发起群留下一个明确的完成回执：首次回填卡写明审核结果回填数、客户通过待确认通知数和待核数；二次机构通知卡写明已发机构、卡片数、达人记录数和待核数。正常阶段只发结果卡；若明细表或卡片交付失败但业务写入已发生，只发一条包含真实数量的短文本，不展开逐条内容。

## 机构通知

1. 只通知本批新增或变化的“客户通过”；客户不通过照常回填，但不进入机构卡。相同审核版本不重复发送。
2. 回填成功后，只在原内部会话发送一张 Card 2.0 二次确认卡并艾特原发起人。卡内按机构分组预览客户通过达人、唯一 ID 和客户反馈；客户不通过不展示。
3. 只有原发起人点击“确认按机构发送”或引用该卡回复“确认发送”，才可进入机构投递。其他成员操作无效；卡片 12 小时过期后必须重新预览。
4. 正式投递时每家机构一张 Card 2.0，表格列出达人、唯一 ID、客户通过结果、客户反馈，并提供机构表按钮。最多十条一张，超过时按机构分批；机构、群聊和卡内达人必须三方一致，任何映射变化都暂缓，不改投、不混机构。通过但需换选题仍发送，并把换题要求放在反馈中。
5. 私聊版式测试明确标为“仅版式预览”，不会回填业务表、不会发送到机构群。
6. 自建的客户通过项进入内部汇总；金铲子群映射未验证时显示“已回填、卡片待配置”，不猜群。
7. 发送失败只重试卡片，不重复回填；结果不确定时保留回执，不盲目重发。重复点击或多个预览覆盖同一审核版本时，以持久化发送回执为准，不重复通知。

遵守共享写锁、应急停止和原发起人确认。不得借此修订客户原稿、外提新达人或直接改全量表。
